# Polymorphism :- Having many forms based on the situations it's behaviour differently
# len() method is polymorphism
# + operator as a polymorphism
# method overloading
# method overriding
# constructor overloading
# len()
# find the length of list and str and dict

# len() function is to finding the length of the list.
# len() is to finding the length of the keys.
# len() is to finding the length pf the strings.
l1 = [10, 20, 30, 40, 50]
print(len(l1))  # 5 (finding the length od the list)

d1 = {1: 2, 2: 3, 3: 4, 4: 5}
print(len(d1)) # 4 (finding the length of the dictionary)

s1 = "Python"
print(len(s1)) # 6 (finding the length of the string)


